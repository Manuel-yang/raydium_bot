export type CoinInfo = {
  symbol: string,
  name: string,
  liquidity: number,
  holder_count: number,
  market_cap: number,
  top_10_holder_rate: string
  renounced_mint: number,
  renounced_freeze_account: number,
  burn_ratio: string,
}